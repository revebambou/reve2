<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.uu.mapper.UserInfoMapper">
  <select id="findall" resultType="com.uu.entity.UserInfo">
      select * from userinfo
  </select>

    <insert id="adduser"  parameterType="com.uu.entity.UserInfo">
       INSERT INTO userinfo(id, name, age, sex) VALUES ((select MAX(id) + 1 from userinfo),#{name},#{age},#{sex})
    </insert>

    <select id="findByid" parameterType="java.lang.Integer" resultType="com.uu.entity.UserInfo">
      SELECT  * FROM userinfo where id = #{id}
    </select>
    
    <update id="updateUser" parameterType="com.uu.entity.UserInfo">
      update  userinfo SET name=#{name },age =#{age},sex=#{sex} WHERE id = #{id}
    </update>

    <delete id="delUser" parameterType="java.lang.Integer">
     DELETE from userinfo WHERE id = #{id}
    </delete>
</mapper>